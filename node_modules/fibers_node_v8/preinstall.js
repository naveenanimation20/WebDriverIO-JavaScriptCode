const fs = require('fs')

const [major, minor, patch] = process.version.match(/^v(\d+\.\d+.\d+)/)[1].split('.')

/**
 * don't install fibers if node version is bigger than 10
 */
if (major >= 10) {
  fs.writeFileSync(__dirname + '/build.js', 'console.log("ignore install")', 'utf8')
}
